# Okia Backend: Full Workflow Documentation

এই ডকুমেন্টটি Okia সিস্টেমের ব্যাকএন্ডের সম্পূর্ণ কার্যপ্রনালী (Full Workflow) ব্যাখ্যা করে। এতে ক্যান্ডিডেট ইমপোর্ট থেকে শুরু করে রেডিয়াস-ভিত্তিক ফিল্টারিং এবং AI ইমেইল জেনারেশন পর্যন্ত প্রতিটি ধাপ বিস্তারিত আলোচনা করা হয়েছে।

---

## ১. ক্যান্ডিডেট ইমপোর্ট ও জিওকোডিং (Candidate Import & Geocoding)

### ধাপসমূহ:
1.  **CV আপলোড**: ইউজার একসাথে ১০০টি পর্যন্ত সিভি (PDF) আপলোড করতে পারেন।
2.  **কিউ প্রসেসিং (P-Queue)**: সার্ভার ক্র্যাশ এড়াতে প্রতিটি সিভি ব্যাকগ্রাউন্ডে একে একে প্রসেস করা হয়।
3.  **ডাটা এক্সট্রাকশন**: `pdf-parse` এবং `Tesseract.js (OCR)` ব্যবহার করে সিভির টেক্সট পড়া হয় এবং নাম, ইমেইল, ফোন ও ঠিকানা বের করা হয়।
4.  **অটোমেটিক জিওকোডিং**: সিভিতে যদি কোনো অ্যাড্রেস পাওয়া যায়, তবে সিস্টেম অটোমেটিক **Nominatim (OpenStreetMap)** API ব্যবহার করে ওই অ্যাড্রেসের **Latitude** এবং **Longitude** খুঁজে বের করে এবং ডাটাবেজে সেভ করে।
5.  **ডুপ্লিকেট চেক**: ইমেইল বা ফোন নাম্বার অলরেডি ডাটাবেজে থাকলে সেই ক্যান্ডিডেটকে স্কিপ করা হয়।

---

## ২. কন্টাক্ট অর্গানাইজেশন ইমপোর্ট (Contact & Organization Import)

### ধাপসমূহ:
1.  **Excel/Bulk Import**: ইউজার এক্সেল ফাইলের মাধ্যমে বিভিন্ন অর্গানাইজেশনের কন্টাক্ট ডিটেইলস ইমপোর্ট করতে পারেন।
2.  **অর্গানাইজেশন জিওকোডিং**: ইমপোর্ট করার সময় অর্গানাইজেশনের অ্যাড্রেস থেকে অটোমেটিক Latitude এবং Longitude জেনারেট করে সেভ করা হয়। এটি পরবর্তী রেডিয়াস-ভিত্তিক ফিল্টারিংয়ের জন্য অত্যন্ত গুরুত্বপূর্ণ।

---

## ৩. রেডিয়াস-ভিত্তিক ফিল্টারিং (Radius-Based Contact Filtering)

সিস্টেম এখন ক্যান্ডিডেটের লোকেশন থেকে নির্দিষ্ট দূরত্বের মধ্যে থাকা অর্গানাইজেশনগুলোকে খুঁজে বের করতে পারে।

### কার্যপ্রনালী:
1.  **API কল**: ইউজার `radius` (কিমি) এবং `generatedCvId` প্যারামিটার দিয়ে কন্টাক্ট লিস্ট রিকোয়েস্ট করেন।
2.  **ক্যান্ডিডেট লোকেশন**: সিস্টেম ওই CV-র সাথে যুক্ত ক্যান্ডিডেটের Latitude এবং Longitude খুঁজে বের করে।
3.  **দূরত্ব গণনা (Haversine Formula)**: সিস্টেম প্রতিটি অর্গানাইজেশনের লোকেশনের সাথে ক্যান্ডিডেটের লোকেশনের দূরত্ব গণনা করে।
    -   সূত্র: Haversine Formula (এটি পৃথিবীর বক্রতা বিবেচনা করে সঠিক দূরত্ব বের করে)।
4.  **ফিল্টারিং**: শুধুমাত্র সেই কন্টাক্টগুলো রিটার্ন করা হয় যারা ক্যান্ডিডেটের লোকেশন থেকে নির্দিষ্ট কিমি (যেমন ৫ কিমি) ব্যাসার্ধের মধ্যে আছে।
5.  **রেসপন্স**: রিটার্ন করা ডাটাতে প্রতিটি কন্টাক্টের জন্য একটি `distance` ফিল্ড থাকে, যা দেখে বোঝা যায় ক্যান্ডিডেট থেকে অর্গানাইজেশনটি কত দূরে।

---

## ৪. AI CV ও ইমেইল জেনারেশন (AI CV & Email Generation)

### ধাপসমূহ:
1.  **CV জেনারেশন**: ইউজার যখন একটি ক্যান্ডিডেট সিলেক্ট করেন, সিস্টেম AI-র মাধ্যমে ওই ক্যান্ডিডেটের জন্য একটি প্রফেশনাল এবং ফরম্যাটেড CV ডাটা জেনারেট করে।
2.  **PDF জেনারেশন**: সিস্টেম অটোমেটিক ওই ডাটা থেকে একটি প্রফেশনাল PDF ফাইল জেনারেট করে এবং সার্ভারে সেভ করে।
3.  **ইমেইল কন্টেন্ট জেনারেশন**: AI ক্যান্ডিডেটের স্কিল এবং অভিজ্ঞতার ওপর ভিত্তি করে একটি কাস্টম সাবজেক্ট, স্যালুটেশন এবং হাইলাইটস তৈরি করে।
4.  **সিগনেচার ব্লক**: প্রতিটি ইমেইলে সেন্ডারের পূর্ণাঙ্গ তথ্য (নাম, পদবী, ফোন, মোবাইল, অ্যাড্রেস, ওয়েবসাইট) যুক্ত করা হয়।

---

## ৫. ইমেইল পাঠানো (Email Dispatch Workflow)

### ধাপসমূহ:
1.  **সিকুয়েন্সিয়াল সেন্ডিং (Serial Sending)**: SendGrid-এর লিমিটেশন এবং ট্র্যাকিংয়ের সুবিধার জন্য ইমেইলগুলো একসাথে না পাঠিয়ে একে একে (One by One) পাঠানো হয়।
2.  **অটো-অ্যাটাচমেন্ট**: সিস্টেম অটোমেটিক চেক করে যদি জেনারেটেড CV PDF না থাকে, তবে অন-দ্য-ফ্লাই জেনারেট করে সেটি অ্যাটাচ করে দেয়। ফাইলের নাম ক্যান্ডিডেটের প্রথম নামে (e.g. `Ahanaf.pdf`) পরিবর্তন করা হয়।
3.  **ব্র্যান্ডিং ও লোগো**: ইমেইলের শেষে Edukai ব্র্যান্ডের ইনফো লোগো (`logoEKAI`) যুক্ত করা হয় এবং লিগ্যাল ডিসক্লেইমার যোগ করা হয়।
4.  **কনসোল লগিং**: প্রতিটি ইমেইল পাঠানোর পর কনসোলে স্ট্যাটাস আপডেট দেওয়া হয় (e.g. `Successfully sent email to: test@example.com`)।

---

## এনভায়রনমেন্ট কনফিগারেশন (Configuration)

সিস্টেমটি সঠিকভাবে চালানোর জন্য `.env` ফাইলে নিচের কি-গুলো থাকা আবশ্যক:
-   `CV_QUEUE_RETRY`: ব্যাকগ্রাউন্ড প্রসেসিংয়ের রিটার্ন সংখ্যা (Default: 2)।
-   `SENDGRID_API_KEY`: SendGrid থেকে মেইল পাঠানোর কি।
-   `SENDGRID_FROM_EMAIL`: অফিসিয়াল সেন্ডার ইমেইল।
-   `AI_API_URL`: AI মডেলের ইউআরএল।
-   `NOMINATIM_USER_AGENT`: জিওকোডিংয়ের জন্য ইউনিক ইউজার এজেন্ট।

---

## টেকনিক্যাল স্ট্যাক (Tech Stack)

-   **Backend**: Node.js, Express, TypeScript.
-   **Database**: PostgreSQL with Prisma ORM.
-   **PDF Generation**: PDFKit.
-   **Geocoding**: OpenStreetMap (Nominatim).
-   **Emailing**: SendGrid, EJS Templates.
-   **Queue**: P-Queue.
-   **OCR**: Tesseract.js.
